import { EditorPlaceholder } from "@/editors/EditorPlaceholder";

export function ImageEditor(): JSX.Element {
  return <EditorPlaceholder name="Image" tagline="Layers, adjustments, annotate, export." />;
}
