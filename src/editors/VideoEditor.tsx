import { EditorPlaceholder } from "@/editors/EditorPlaceholder";

export function VideoEditor(): JSX.Element {
  return <EditorPlaceholder name="Video" tagline="Timeline, storyboard, 4K export." />;
}
